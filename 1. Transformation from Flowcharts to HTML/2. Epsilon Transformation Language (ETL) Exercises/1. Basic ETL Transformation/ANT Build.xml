<?xml version="1.0"?>

<!--
	The following build script illustrates how to launch the example via ANT.
	Ensure that External Tools Configuration specifies that the JRE to be used
	is the same as the workspace.
		1) Click External Tools Configuration... from the External Tools dropdown.
		2) Select the ANT build for this build script.
		3) Click the JRE tab.
		4) Select the option that reads: "Run in the same JRE as the workspace."
		5) Click Apply.
		6) Click Run.
-->

<project default="main">
  <epsilon.emf.register file="../../0. Metamodels/Flowchart.ecore" />
  <epsilon.emf.register file="../../0. Metamodels/HTML.ecore" />

  <target name="loadModels">
    <epsilon.emf.loadModel name="Flowchart"
      modelFile="../../1. Models/Wake-up Process.model"
      metamodelUri="flowchart"
      read="true" store="false" />

    <epsilon.emf.loadModel name="HTML"
      modelFile="HTML Model.model"
      metamodelUri="HTML"
      read="false" store="true" />
  </target>

  <target name="main" depends="loadModels">
    <epsilon.etl src="Basic Rules.etl">
      <model ref="Flowchart" />
      <model ref="HTML" />
    </epsilon.etl>
  </target>
</project>